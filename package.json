{
  "workspaces": [
    "web",
    "bot",
    "shared"
  ],
  "dependencies": {
    "pg": "^8.16.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  },
  "scripts": {
    "start": "npm run start --workspace=bot",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=bot"
  }
}
