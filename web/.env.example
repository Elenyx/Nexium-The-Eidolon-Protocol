# Discord OAuth2 Configuration
# Get these from https://discord.com/developers/applications

# Your Discord application/client ID (required)
DISCORD_CLIENT_ID=123

# Your Discord application/client secret (required)
DISCORD_CLIENT_SECRET=123

# Discord OAuth2 redirect URI (required for production)
# For Railway: https://your-app-name.railway.app/api/auth/discord/callback
# For Netlify: https://your-site-name.netlify.app/api/auth/discord/callback
# For local development: http://localhost:5000/api/auth/discord/callback
DISCORD_REDIRECT_URI=http://localhost:5000/api/auth/discord/callback

# Database Configuration
# Neon PostgreSQL connection string (required)
# Get from https://console.neon.tech/
DATABASE_URL=postgresql://username:password@host:port/database

# Server Configuration
# Port for the server (optional, defaults to 5000)
PORT=5000

# Environment
# Set to 'production' for production deployment
NODE_ENV=environment

# Replit Configuration (if deploying to Replit)
# REPLIT_DOMAINS=your-replit-domain.replit.dev
